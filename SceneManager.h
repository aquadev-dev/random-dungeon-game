#pragma once

using namespace std;

class SceneManager // SceneManager for managing the current scene
{
public:
	SceneManager(); // Constructor initializes the scene manager
	~SceneManager(); // Destructor cleans up the scene manager

	void LoadScene(int sceneNumber); // Loads a scene based on the scene number


	
};

